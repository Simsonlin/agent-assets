# Matt codebase-design 首次试用

- 日期：2026-09-07；宿主：Codex 桌面随附 CLI 0.153.4。
- 任务：只读检查 design-workflow 预览生成与静态编辑回写的模块接口，最多两项观察；不修改代码、不运行构建、不访问外部应用、不启动子 Agent。
- 用户授权：“允许这一次限定范围的 Codex 试用”，范围为 README、文档导航和预览生成/静态编辑回写相关源码。首次模型请求遇到用量限制；后续自动审查要求明确数据范围授权，获得上述回复后重试成功。
- 资产：matt/codebase-design，固定版本见 record.json；临时名 trial-matt-codebase-design-51ca68。完整支持文件一起准备，上游副本未改写。
- 原生发现：verified，依据宿主报告对可用清单的明确确认；未另行导出完整目录。
- 原生调用：verified，依据显式调用、对应正文和 DEEPENING.md 成功读取，以及最终报告对该方法定义的实际使用。见 [执行证据](native-evidence.json)。
- 结果：limited。完成一次真实静态检查；方法有可见帮助，但未做无 Skill 对照，也未运行构建复现，不能据此认定跨任务收益或缺陷影响。
- 报告提出两个观察：入口文档与当前约束不一致；普通构建和独立基线生成装配的插件不同。主 Agent 复核了所引行号，确认文档差异及插件列表差异；第二项导致失败仍是待运行验证的推断。见 [报告](report.md)。
- 帮助：将文档、配置和顺序都看作调用者接口负担，建议限定在既有模块，无需凭空发明抽象。
- 限制：本次仅覆盖单一阅读任务和一个 Matt Skill，不代表整套 Matt Workflow 已试过；临时名称转换后的使用也不等于所有宿主原名调用方式均已验证。
- 撤销：verify 摘要一致；cleanup 返回 cleaned，protected 为空。原项目产品文件不属于试用修改范围。
- 建议：保留原版继续试用，暂不特化，不自动正式纳入；下次有真实设计修改任务时再检验建议是否有用。
