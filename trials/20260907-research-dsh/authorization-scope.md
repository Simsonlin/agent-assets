# DSH 真实材料试用的授权范围

状态：用户要求暂时跳过 DSH，尚未运行模型试用；未取得数据发送授权。

目的：使用已准备的关键假设检查 Skill，只读检查 W04 中“下发前完成整套准备可减少一线返工”的假设。

宿主和路由：本机 DSH 0.1.2-alpha.2；用户现有默认 provider=deepseek-official、model=deepseek-v4-flash、reasoningEffort=high。模型请求由现有官方 provider 发出，凭据仅由宿主读取，不输出或复制。

本次提示：[planned-prompt.txt](planned-prompt.txt)。允许读取的项目文件范围为 AGENTS.md、00-项目工作台.md、工作方向/W04-大型银行对公商机治理研究.md、工作方向/W04-商机全链条标准范式-v0.1.md。问题所需内容会进入模型请求；不读取其他业务材料，不修改正式判断，不联网开展新研究，不调用外部应用。

自动审批审查拒绝了首次运行请求，理由是项目潜在敏感材料尚未获得向该模型服务发送的具体授权。运行命令未启动；用户确认前不重试真实数据路线。
