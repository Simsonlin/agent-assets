# 本地差异

## 当前状态

2026-09-06：上游文件无修改。`upstream/` 中的 69 个文件均从固定提交逐字节复制，包括 17 个 `SKILL.md`、调用元数据、支持文件和对应说明。文件级基线见 [SHA256SUMS](SHA256SUMS)。

本地仅做选集打包和中文文档编写。选集遗漏的上游导航目标不通过修改原文修复，按 [SOURCE.md](SOURCE.md) 中的固定版本链接访问。

## 使用建议与源文件修改的区别

[GUIDE.md](GUIDE.md) 中的项目级安装建议、调用示例、审查范围检查、人工收尾与轻量试用记录都是本地建议，不是新增的上游规则。它们不会自动注入安装后的 skill。

一次一问、暂不提交、真实插件实验等个别请求可以在当次调用中覆盖默认行为。将其记入试用记录；只有之后确实修改了 skill 文件，才算源码补丁。

## 以后有补丁时记录

每项实际修改记录：日期、文件、上游基线 commit、触发它的真实试用记录或上游 issue、原行为与新行为、验证结果、何时可撤销。保持一个问题对应一项可解释的修改。

校验和用于识别相对上游基线的差异。不要在本地微调后重算基线来抹掉差异；只有整体切换上游版本时才重新建立基线。
